<h1>Пользователи, добавленные из формы</h1>

<table>
    <?php foreach ($users as $array): ?>
            <tr>
                <td>
                    <?= $array['id'] ?>
                    <?= $array['name'] ?>
                    <?= $array['email'] ?>
                </td>
            </tr>
    <?php endforeach ?>
</table>