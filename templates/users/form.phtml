<h1>Пользователи, добавленные из формы</h1>

<table>
    <?php foreach ($users as $user): ?>
        <tr>
            <td>
                <?= $user ?>
            </td>
        </tr>
    <?php endforeach ?>
</table>